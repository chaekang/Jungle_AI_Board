# Infra

This folder holds shared local infrastructure.

## PostgreSQL

Start the local PostgreSQL container from the repository root:

```powershell
docker compose -f infra/docker-compose.yml up -d
```
