# 009_rag

## 목표

이 문서는 [implementation_order.md](../implementation_order.md)의 `10. RAG 기능`을 실제 코드로 완성하기 위한 작업 문서다.

이 파일만 보고 따라 하면 아래 기능까지 완성되는 것을 목표로 한다.

- 좌석 후기 하나를 AI가 읽기 좋은 검색 문서로 변환한다.
- 후기 생성/수정 후 임베딩을 만들어 DB에 저장한다.
- 사용자의 자연어 질문에서 극장, 작품, 층, 구역, 열, 번호, 의도 조건을 뽑는다.
- 구조화 필터와 벡터 검색을 함께 사용해 관련 후기를 찾는다.
- 찾은 후기만 LLM에 전달해 답변을 만든다.
- 답변과 근거 후기 목록을 `POST /rag/questions`에서 반환한다.

관련 개념 문서:

- [docs/step 1/concept/009_rag.md](concept/009_rag.md)
- OpenAI Embeddings API: <https://developers.openai.com/api/docs/guides/embeddings>
- OpenAI Responses API: <https://developers.openai.com/api/docs/guides/text>

## 이번 단계의 구현 방향

현재 프로젝트는 실제 도메인 데이터가 `apps/nest-api`의 Prisma/Nest 서비스에 모여 있다.
따라서 1차 RAG MVP는 `apps/nest-api` 안에 구현한다.

`apps/fastapi-api`는 나중에 별도 AI 서버, 실험용 모델 서버, 배치 처리 서버가 필요해졌을 때 분리한다.
지금 단계에서 FastAPI까지 같이 붙이면 데이터 접근, 인증, 배포 흐름이 둘로 갈라져 오히려 완성이 늦어진다.

이번 문서의 기준 구조는 아래와 같다.

```text
React
  -> POST /rag/questions
Nest RAG Controller
  -> 질문 조건 추출
  -> 질문 임베딩 생성
  -> Postgres pgvector 검색
  -> LLM 답변 생성
  -> answer + reasons + sources 반환
Postgres
  -> seat_reviews
  -> theaters / musicals / performances / tags
  -> seat_review_embeddings
```

## 완성 기준

- `POST /rag/questions`에 질문을 보내면 `answer`, `reasons`, `sources`, `filters`가 돌아온다.
- `sources`에는 실제 후기 id, 극장명, 작품명, 좌석 위치, 평점, 태그, 본문 일부가 들어간다.
- 작품명이 들어간 질문은 해당 작품 후기가 우선 검색된다.
- 극장/좌석 위치가 들어간 질문은 해당 위치 조건이 우선 반영된다.
- 편안함 질문은 `comfortRating`이 높은 후기와 착석감 관련 문서를 더 잘 찾는다.
- 배우/배역/표정 질문은 `expressionRating`, 좌우 방향, 동선 관련 문서를 더 잘 찾는다.
- 임베딩 실패가 후기 작성 자체를 막지 않는다.

## 파일 경로 규칙

이 단계에서 추가하거나 수정할 파일은 아래 위치를 사용한다.

- Nest RAG 코드: `apps/nest-api/src/rag`
- RAG DTO: `apps/nest-api/src/rag/dto`
- Prisma 스키마: `apps/nest-api/prisma/schema.prisma`
- Prisma 마이그레이션: `apps/nest-api/prisma/migrations/<timestamp>_add_rag_embeddings/migration.sql`
- RAG 백필 스크립트: `apps/nest-api/prisma/backfill-rag-embeddings.ts`
- React API 함수: `apps/web-react/src/features/rag/api.ts`
- React 타입: `apps/web-react/src/features/rag/types.ts`

## 1. 환경변수 추가

`apps/nest-api/.env` 또는 실행 환경에 아래 값을 추가한다.

```env
OPENAI_API_KEY=sk-...
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_CHAT_MODEL=gpt-5.5
```

`text-embedding-3-small`의 기본 임베딩 차원은 `1536`이다.
이 문서의 `pgvector` 컬럼도 `vector(1536)`으로 만든다.

모델 접근 권한이 없으면 `OPENAI_CHAT_MODEL`은 현재 계정에서 사용할 수 있는 텍스트 생성 모델명으로 바꾼다.
코드는 모델명을 환경변수로 받으므로 나중에 교체해도 RAG 로직은 그대로 둔다.

## 2. pgvector 테이블 추가

Postgres에서 벡터 검색을 하려면 `pgvector` 확장이 필요하다.
Docker DB를 쓰고 있다면 이미지에 `pgvector`가 포함되어 있는지 먼저 확인한다.

확인이 필요한 SQL:

```sql
SELECT * FROM pg_available_extensions WHERE name = 'vector';
```

없다면 DB 이미지 또는 확장 설치 방법을 먼저 정리해야 한다.
있다면 아래 마이그레이션을 추가한다.

파일 생성:

```text
apps/nest-api/prisma/migrations/20260613100000_add_rag_embeddings/migration.sql
```

내용:

```sql
CREATE EXTENSION IF NOT EXISTS vector;

CREATE TABLE "seat_review_embeddings" (
  "seat_review_id" BIGINT PRIMARY KEY,
  "document" TEXT NOT NULL,
  "embedding" vector(1536) NOT NULL,
  "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updated_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "seat_review_embeddings_seat_review_id_fkey"
    FOREIGN KEY ("seat_review_id")
    REFERENCES "seat_reviews"("id")
    ON DELETE CASCADE
    ON UPDATE CASCADE
);

CREATE INDEX "seat_review_embeddings_embedding_idx"
ON "seat_review_embeddings"
USING ivfflat ("embedding" vector_cosine_ops)
WITH (lists = 100);

CREATE INDEX "seat_review_embeddings_updated_at_idx"
ON "seat_review_embeddings"("updated_at");
```

마이그레이션 실행:

```bash
cd apps/nest-api
npx prisma migrate dev
```

## 3. Prisma schema 수정

`apps/nest-api/prisma/schema.prisma`의 `SeatReview` 모델에 `embedding` 관계를 추가한다.

```prisma
model SeatReview {
  id                    BigInt      @id @default(autoincrement())
  authorId              BigInt      @map("author_id")
  theaterId             BigInt      @map("theater_id")
  musicalId             BigInt      @map("musical_id")
  performanceId         BigInt?     @map("performance_id")
  seatFloor             String      @map("seat_floor")
  seatSection           String?     @map("seat_section")
  seatRow               String      @map("seat_row")
  seatNumber            String      @map("seat_number")
  viewRating            Int         @map("view_rating")
  soundRating           Int         @map("sound_rating")
  comfortRating         Int         @map("comfort_rating")
  expressionRating      Int         @map("expression_rating")
  stageVisibilityRating Int         @map("stage_visibility_rating")
  content               String
  createdAt             DateTime    @default(now()) @map("created_at")
  updatedAt             DateTime    @updatedAt @map("updated_at")

  author                User        @relation(fields: [authorId], references: [id])
  theater               Theater     @relation(fields: [theaterId], references: [id])
  musical               Musical     @relation(fields: [musicalId], references: [id])
  performance           Performance? @relation(fields: [performanceId], references: [id])

  comments              Comment[]
  seatReviewTags        SeatReviewTag[]
  embedding             SeatReviewEmbedding?

  @@index([theaterId])
  @@index([musicalId])
  @@index([performanceId])
  @@index([seatFloor])
  @@index([seatSection])
  @@index([createdAt])
  @@map("seat_reviews")
}
```

그리고 파일 아래쪽에 새 모델을 추가한다.

```prisma
model SeatReviewEmbedding {
  seatReviewId BigInt   @id @map("seat_review_id")
  document     String
  embedding    Unsupported("vector(1536)")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  seatReview   SeatReview @relation(fields: [seatReviewId], references: [id], onDelete: Cascade)

  @@index([updatedAt])
  @@map("seat_review_embeddings")
}
```

수정 후 Prisma Client를 다시 생성한다.

```bash
cd apps/nest-api
npx prisma generate
```

## 4. RAG DTO 추가

파일 생성:

```text
apps/nest-api/src/rag/dto/ask-rag-question.dto.ts
```

```ts
import { Type } from 'class-transformer';
import { IsInt, IsOptional, IsString, Max, Min, MinLength } from 'class-validator';

export class AskRagQuestionDto {
  @IsString()
  @MinLength(2)
  question: string;

  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  @Max(10)
  limit?: number = 5;
}
```

## 5. RAG 타입 추가

파일 생성:

```text
apps/nest-api/src/rag/rag.types.ts
```

```ts
export type RagIntent =
  | 'general'
  | 'view'
  | 'sound'
  | 'comfort'
  | 'expression'
  | 'stageVisibility';

export type RagQuestionFilters = {
  theaterId?: string;
  theaterName?: string;
  musicalId?: string;
  musicalTitle?: string;
  seatFloor?: string;
  seatSection?: string;
  seatRow?: string;
  seatNumber?: string;
  side?: 'left' | 'center' | 'right';
  intent: RagIntent;
};

export type RagSource = {
  id: string;
  score: number;
  theater: {
    id: string;
    name: string;
  };
  musical: {
    id: string;
    title: string;
  };
  performance: {
    id: string;
    seasonLabel: string | null;
  } | null;
  seat: {
    floor: string;
    section: string | null;
    row: string;
    number: string;
  };
  ratings: {
    view: number;
    sound: number;
    comfort: number;
    expression: number;
    stageVisibility: number;
  };
  tags: string[];
  content: string;
};

export type RagAnswer = {
  answer: string;
  reasons: string[];
  filters: RagQuestionFilters;
  sources: RagSource[];
};
```

## 6. 후기 문서 빌더 추가

RAG에서 가장 중요한 것은 “임베딩 전에 어떤 문자열을 만들 것인가”다.
숫자 평점만 넣으면 질문 맥락을 잘 못 잡고, 본문만 넣으면 좌석 위치와 작품 조건을 놓친다.
그래서 후기 하나를 아래처럼 사람이 읽을 수 있는 문서로 만든다.

파일 생성:

```text
apps/nest-api/src/rag/rag-document.builder.ts
```

```ts
import { Prisma } from '@prisma/client';

export const ragSeatReviewInclude = {
  theater: true,
  musical: true,
  performance: true,
  seatReviewTags: {
    include: {
      tag: true,
    },
  },
} satisfies Prisma.SeatReviewInclude;

export type RagSeatReview = Prisma.SeatReviewGetPayload<{
  include: typeof ragSeatReviewInclude;
}>;

export function buildSeatReviewRagDocument(review: RagSeatReview) {
  const tags = review.seatReviewTags.map(({ tag }) => tag.name).join(', ') || '없음';
  const section = review.seatSection ?? '구역 정보 없음';
  const season = review.performance?.seasonLabel ?? '시즌 정보 없음';

  return [
    `후기 ID: ${review.id.toString()}`,
    `극장: ${review.theater.name}`,
    `작품: ${review.musical.title}`,
    `시즌: ${season}`,
    `좌석: ${review.seatFloor} ${section} ${review.seatRow}열 ${review.seatNumber}번`,
    `시야 평점: ${review.viewRating}/5`,
    `음향 평점: ${review.soundRating}/5`,
    `좌석 편안함 평점: ${review.comfortRating}/5`,
    `배우 표정 체감 평점: ${review.expressionRating}/5`,
    `무대 전체 체감 평점: ${review.stageVisibilityRating}/5`,
    `태그: ${tags}`,
    `후기 본문: ${review.content}`,
  ].join('\n');
}
```

## 7. 질문 조건 추출기 추가

처음부터 LLM으로 조건 추출까지 맡기면 비용과 실패 지점이 늘어난다.
MVP에서는 DB에 있는 극장/작품 이름과 간단한 정규식으로 먼저 추출한다.

파일 생성:

```text
apps/nest-api/src/rag/rag-query-parser.ts
```

```ts
import { Injectable } from '@nestjs/common';
import type { RagIntent, RagQuestionFilters } from './rag.types';
import { PrismaService } from 'src/database/prisma.service';

@Injectable()
export class RagQueryParser {
  constructor(private readonly prisma: PrismaService) {}

  async parse(question: string): Promise<RagQuestionFilters> {
    const normalized = question.trim();
    const [theaters, musicals] = await Promise.all([
      this.prisma.theater.findMany({
        select: { id: true, name: true },
        orderBy: { name: 'asc' },
      }),
      this.prisma.musical.findMany({
        select: { id: true, title: true },
        orderBy: { title: 'asc' },
      }),
    ]);

    const theater = theaters.find((item) => normalized.includes(item.name));
    const musical = musicals.find((item) => normalized.includes(item.title));

    return {
      ...(theater
        ? {
            theaterId: theater.id.toString(),
            theaterName: theater.name,
          }
        : {}),
      ...(musical
        ? {
            musicalId: musical.id.toString(),
            musicalTitle: musical.title,
          }
        : {}),
      ...this.extractSeat(normalized),
      side: this.extractSide(normalized),
      intent: this.extractIntent(normalized),
    };
  }

  private extractSeat(question: string) {
    const floorMatch = question.match(/(\d+)\s*층/);
    const sectionMatch = question.match(/([A-Z가-힣0-9]+)\s*(구역|블록)/i);
    const rowMatch = question.match(/(\d+|[A-Z가-힣]+)\s*열/i);
    const numberMatch = question.match(/(\d+)\s*(번|번석|좌석)/);

    return {
      ...(floorMatch ? { seatFloor: `${floorMatch[1]}층` } : {}),
      ...(sectionMatch ? { seatSection: sectionMatch[1].toUpperCase() } : {}),
      ...(rowMatch ? { seatRow: rowMatch[1].toUpperCase() } : {}),
      ...(numberMatch ? { seatNumber: numberMatch[1] } : {}),
    };
  }

  private extractSide(question: string) {
    if (/(왼쪽|좌측|왼블|왼블럭|왼쪽블록)/.test(question)) {
      return 'left' as const;
    }

    if (/(오른쪽|우측|오블|오른블럭|오른쪽블록)/.test(question)) {
      return 'right' as const;
    }

    if (/(중앙|센터|가운데|중블|중앙블록)/.test(question)) {
      return 'center' as const;
    }

    return undefined;
  }

  private extractIntent(question: string): RagIntent {
    if (/(편한|편안|불편|다리|허리|장시간|착석|좌석감|피로)/.test(question)) {
      return 'comfort';
    }

    if (/(음향|소리|오케|오케스트라|스피커|잘 들)/.test(question)) {
      return 'sound';
    }

    if (/(표정|얼굴|배우|배역|동선|왼쪽|오른쪽|좌측|우측)/.test(question)) {
      return 'expression';
    }

    if (/(전체|무대|군무|앙상블|연출|동선 전체)/.test(question)) {
      return 'stageVisibility';
    }

    if (/(시야|보임|가림|난간|앞사람|가려|잘 보여)/.test(question)) {
      return 'view';
    }

    return 'general';
  }
}
```

주의할 점:

- `seatFloor` 저장값이 현재 DB에서 `1F`, `2F`처럼 들어가 있다면 `1층` 추출값을 그대로 쓰면 안 된다.
- 이 경우 `extractSeat`에서 `1층 -> 1F`로 바꾸거나, DB 입력 규칙을 `1층`으로 통일한다.
- 현재 프로젝트의 작성 DTO를 보면 `seatFloor`는 자유 문자열이므로, 팀에서 한 가지 규칙을 먼저 정한다.

## 8. OpenAI 클라이언트 추가

SDK를 추가하지 않고 `fetch`로 구현한다.
Nest 프로젝트에 이미 외부 HTTP 클라이언트 의존성이 없기 때문에, MVP에서는 이 방식이 가장 작다.

파일 생성:

```text
apps/nest-api/src/rag/openai-rag.client.ts
```

```ts
import { Injectable, InternalServerErrorException } from '@nestjs/common';
import type { RagQuestionFilters, RagSource } from './rag.types';

type EmbeddingResponse = {
  data?: Array<{
    embedding?: number[];
  }>;
};

type ResponsesApiResponse = {
  output_text?: string;
  output?: Array<{
    content?: Array<{
      text?: string;
    }>;
  }>;
};

@Injectable()
export class OpenAiRagClient {
  private readonly apiKey = process.env.OPENAI_API_KEY;
  private readonly embeddingModel =
    process.env.OPENAI_EMBEDDING_MODEL ?? 'text-embedding-3-small';
  private readonly chatModel = process.env.OPENAI_CHAT_MODEL ?? 'gpt-5.5';

  async createEmbedding(input: string) {
    this.assertApiKey();

    const response = await fetch('https://api.openai.com/v1/embeddings', {
      method: 'POST',
      headers: {
        Authorization: `Bearer ${this.apiKey}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        model: this.embeddingModel,
        input,
        encoding_format: 'float',
      }),
    });

    if (!response.ok) {
      throw new InternalServerErrorException('Failed to create embedding');
    }

    const data = (await response.json()) as EmbeddingResponse;
    const embedding = data.data?.[0]?.embedding;

    if (!embedding?.length) {
      throw new InternalServerErrorException('Embedding response is empty');
    }

    return embedding;
  }

  async createAnswer(input: {
    question: string;
    filters: RagQuestionFilters;
    sources: RagSource[];
  }) {
    this.assertApiKey();

    const response = await fetch('https://api.openai.com/v1/responses', {
      method: 'POST',
      headers: {
        Authorization: `Bearer ${this.apiKey}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        model: this.chatModel,
        reasoning: { effort: 'low' },
        input: [
          {
            role: 'developer',
            content: [
              '너는 뮤지컬 좌석 후기 기반 추천 도우미다.',
              '반드시 제공된 근거 후기 안에서만 답한다.',
              '근거가 부족하면 부족하다고 말한다.',
              '답변은 한국어로 작성한다.',
              '과장하지 말고, 좌석 위치와 평점, 후기 내용을 함께 근거로 든다.',
            ].join('\n'),
          },
          {
            role: 'user',
            content: this.buildPrompt(input.question, input.filters, input.sources),
          },
        ],
      }),
    });

    if (!response.ok) {
      throw new InternalServerErrorException('Failed to create RAG answer');
    }

    const data = (await response.json()) as ResponsesApiResponse;
    const outputText =
      data.output_text ??
      data.output
        ?.flatMap((item) => item.content ?? [])
        .map((content) => content.text)
        .filter(Boolean)
        .join('\n');

    if (!outputText) {
      throw new InternalServerErrorException('RAG answer response is empty');
    }

    return outputText;
  }

  private buildPrompt(
    question: string,
    filters: RagQuestionFilters,
    sources: RagSource[],
  ) {
    const sourceText = sources
      .map((source, index) =>
        [
          `[근거 ${index + 1}]`,
          `후기 ID: ${source.id}`,
          `극장: ${source.theater.name}`,
          `작품: ${source.musical.title}`,
          `좌석: ${source.seat.floor} ${source.seat.section ?? ''} ${source.seat.row}열 ${source.seat.number}번`,
          `평점: 시야 ${source.ratings.view}, 음향 ${source.ratings.sound}, 편안함 ${source.ratings.comfort}, 표정 ${source.ratings.expression}, 전체무대 ${source.ratings.stageVisibility}`,
          `태그: ${source.tags.join(', ') || '없음'}`,
          `후기: ${source.content}`,
        ].join('\n'),
      )
      .join('\n\n');

    return [
      `사용자 질문: ${question}`,
      `추출된 조건: ${JSON.stringify(filters)}`,
      '',
      '관련 후기:',
      sourceText || '관련 후기가 없음',
      '',
      '답변 형식:',
      '1. 먼저 한 문단으로 결론을 말한다.',
      '2. 그 다음 근거를 2-4개 bullet로 정리한다.',
      '3. 마지막에 주의할 점이나 추가로 확인하면 좋은 조건을 짧게 말한다.',
    ].join('\n');
  }

  private assertApiKey() {
    if (!this.apiKey) {
      throw new InternalServerErrorException('OPENAI_API_KEY is not set');
    }
  }
}
```

## 9. RAG 서비스 추가

파일 생성:

```text
apps/nest-api/src/rag/rag.service.ts
```

```ts
import {
  BadRequestException,
  Injectable,
  InternalServerErrorException,
} from '@nestjs/common';
import { Prisma } from '@prisma/client';
import { PrismaService } from 'src/database/prisma.service';
import { OpenAiRagClient } from './openai-rag.client';
import { buildSeatReviewRagDocument, ragSeatReviewInclude } from './rag-document.builder';
import { RagQueryParser } from './rag-query-parser';
import type { RagAnswer, RagQuestionFilters, RagSource } from './rag.types';

type RagCandidateRow = {
  id: bigint;
  distance: number;
  theater_id: bigint;
  theater_name: string;
  musical_id: bigint;
  musical_title: string;
  performance_id: bigint | null;
  season_label: string | null;
  seat_floor: string;
  seat_section: string | null;
  seat_row: string;
  seat_number: string;
  view_rating: number;
  sound_rating: number;
  comfort_rating: number;
  expression_rating: number;
  stage_visibility_rating: number;
  content: string;
  tags: string[];
};

@Injectable()
export class RagService {
  constructor(
    private readonly prisma: PrismaService,
    private readonly openAi: OpenAiRagClient,
    private readonly queryParser: RagQueryParser,
  ) {}

  async ask(question: string, limit = 5): Promise<RagAnswer> {
    const normalizedQuestion = question.trim();

    if (normalizedQuestion.length < 2) {
      throw new BadRequestException('question must be at least 2 characters');
    }

    const filters = await this.queryParser.parse(normalizedQuestion);
    const queryEmbedding = await this.openAi.createEmbedding(
      this.buildQueryText(normalizedQuestion, filters),
    );
    const sources = await this.searchSources(queryEmbedding, filters, limit);

    if (sources.length === 0) {
      return {
        answer: '아직 이 질문에 답할 만큼 관련 좌석 후기가 충분하지 않습니다.',
        reasons: ['검색 조건과 의미가 모두 맞는 후기를 찾지 못했습니다.'],
        filters,
        sources: [],
      };
    }

    const answer = await this.openAi.createAnswer({
      question: normalizedQuestion,
      filters,
      sources,
    });

    return {
      answer,
      reasons: this.buildReasons(filters, sources),
      filters,
      sources,
    };
  }

  async upsertReviewEmbedding(reviewId: bigint) {
    const review = await this.prisma.seatReview.findUnique({
      where: { id: reviewId },
      include: ragSeatReviewInclude,
    });

    if (!review) {
      throw new BadRequestException('Seat review not found');
    }

    const document = buildSeatReviewRagDocument(review);
    const embedding = await this.openAi.createEmbedding(document);
    const vector = this.toVectorLiteral(embedding);

    await this.prisma.$executeRaw`
      INSERT INTO "seat_review_embeddings" ("seat_review_id", "document", "embedding", "updated_at")
      VALUES (${reviewId}, ${document}, ${vector}::vector, NOW())
      ON CONFLICT ("seat_review_id")
      DO UPDATE SET
        "document" = EXCLUDED."document",
        "embedding" = EXCLUDED."embedding",
        "updated_at" = NOW()
    `;

    return {
      seatReviewId: reviewId.toString(),
      indexed: true,
    };
  }

  async deleteReviewEmbedding(reviewId: bigint) {
    await this.prisma.$executeRaw`
      DELETE FROM "seat_review_embeddings"
      WHERE "seat_review_id" = ${reviewId}
    `;
  }

  async reindexAll() {
    const reviews = await this.prisma.seatReview.findMany({
      select: { id: true },
      orderBy: { id: 'asc' },
    });

    for (const review of reviews) {
      await this.upsertReviewEmbedding(review.id);
    }

    return {
      indexedCount: reviews.length,
    };
  }

  private async searchSources(
    embedding: number[],
    filters: RagQuestionFilters,
    limit: number,
  ): Promise<RagSource[]> {
    const vector = this.toVectorLiteral(embedding);
    const conditions = this.buildSqlConditions(filters);
    const orderBy = this.buildOrderBy(filters);
    const safeLimit = Math.min(Math.max(limit, 1), 10);

    const rows = await this.prisma.$queryRaw<RagCandidateRow[]>(Prisma.sql`
      SELECT
        sr."id",
        (sre."embedding" <=> ${vector}::vector) AS "distance",
        t."id" AS "theater_id",
        t."name" AS "theater_name",
        m."id" AS "musical_id",
        m."title" AS "musical_title",
        p."id" AS "performance_id",
        p."season_label",
        sr."seat_floor",
        sr."seat_section",
        sr."seat_row",
        sr."seat_number",
        sr."view_rating",
        sr."sound_rating",
        sr."comfort_rating",
        sr."expression_rating",
        sr."stage_visibility_rating",
        sr."content",
        COALESCE(array_remove(array_agg(tag."name"), NULL), ARRAY[]::text[]) AS "tags"
      FROM "seat_review_embeddings" sre
      JOIN "seat_reviews" sr ON sr."id" = sre."seat_review_id"
      JOIN "theaters" t ON t."id" = sr."theater_id"
      JOIN "musicals" m ON m."id" = sr."musical_id"
      LEFT JOIN "performances" p ON p."id" = sr."performance_id"
      LEFT JOIN "seat_review_tags" srt ON srt."seat_review_id" = sr."id"
      LEFT JOIN "tags" tag ON tag."id" = srt."tag_id"
      ${conditions.length > 0 ? Prisma.sql`WHERE ${Prisma.join(conditions, ' AND ')}` : Prisma.empty}
      GROUP BY sr."id", sre."embedding", t."id", m."id", p."id"
      ${orderBy}
      LIMIT ${safeLimit}
    `);

    return rows.map((row) => ({
      id: row.id.toString(),
      score: Number((1 - row.distance).toFixed(4)),
      theater: {
        id: row.theater_id.toString(),
        name: row.theater_name,
      },
      musical: {
        id: row.musical_id.toString(),
        title: row.musical_title,
      },
      performance: row.performance_id
        ? {
            id: row.performance_id.toString(),
            seasonLabel: row.season_label,
          }
        : null,
      seat: {
        floor: row.seat_floor,
        section: row.seat_section,
        row: row.seat_row,
        number: row.seat_number,
      },
      ratings: {
        view: row.view_rating,
        sound: row.sound_rating,
        comfort: row.comfort_rating,
        expression: row.expression_rating,
        stageVisibility: row.stage_visibility_rating,
      },
      tags: row.tags ?? [],
      content: row.content,
    }));
  }

  private buildSqlConditions(filters: RagQuestionFilters) {
    const conditions: Prisma.Sql[] = [];

    if (filters.theaterId) {
      conditions.push(Prisma.sql`sr."theater_id" = ${BigInt(filters.theaterId)}`);
    }

    if (filters.musicalId) {
      conditions.push(Prisma.sql`sr."musical_id" = ${BigInt(filters.musicalId)}`);
    }

    if (filters.seatFloor) {
      conditions.push(Prisma.sql`sr."seat_floor" ILIKE ${filters.seatFloor}`);
    }

    if (filters.seatSection) {
      conditions.push(Prisma.sql`sr."seat_section" ILIKE ${filters.seatSection}`);
    }

    if (filters.seatRow) {
      conditions.push(Prisma.sql`sr."seat_row" ILIKE ${filters.seatRow}`);
    }

    if (filters.seatNumber) {
      conditions.push(Prisma.sql`sr."seat_number" ILIKE ${filters.seatNumber}`);
    }

    return conditions;
  }

  private buildOrderBy(filters: RagQuestionFilters) {
    switch (filters.intent) {
      case 'comfort':
        return Prisma.sql`ORDER BY sr."comfort_rating" DESC, "distance" ASC`;
      case 'sound':
        return Prisma.sql`ORDER BY sr."sound_rating" DESC, "distance" ASC`;
      case 'expression':
        return Prisma.sql`ORDER BY sr."expression_rating" DESC, "distance" ASC`;
      case 'stageVisibility':
        return Prisma.sql`ORDER BY sr."stage_visibility_rating" DESC, "distance" ASC`;
      case 'view':
        return Prisma.sql`ORDER BY sr."view_rating" DESC, "distance" ASC`;
      case 'general':
      default:
        return Prisma.sql`ORDER BY "distance" ASC`;
    }
  }

  private buildQueryText(question: string, filters: RagQuestionFilters) {
    return [
      `질문: ${question}`,
      filters.theaterName ? `극장 조건: ${filters.theaterName}` : undefined,
      filters.musicalTitle ? `작품 조건: ${filters.musicalTitle}` : undefined,
      filters.seatFloor ? `층 조건: ${filters.seatFloor}` : undefined,
      filters.seatSection ? `구역 조건: ${filters.seatSection}` : undefined,
      filters.seatRow ? `열 조건: ${filters.seatRow}` : undefined,
      filters.side ? `좌우 방향 조건: ${filters.side}` : undefined,
      `질문 의도: ${filters.intent}`,
    ]
      .filter(Boolean)
      .join('\n');
  }

  private buildReasons(filters: RagQuestionFilters, sources: RagSource[]) {
    const reasons: string[] = [];

    if (filters.theaterName) {
      reasons.push(`${filters.theaterName} 극장 후기를 우선 검색했습니다.`);
    }

    if (filters.musicalTitle) {
      reasons.push(`${filters.musicalTitle} 작품 후기를 우선 검색했습니다.`);
    }

    if (filters.intent !== 'general') {
      reasons.push(`${filters.intent} 의도에 맞는 평점 기준을 검색 정렬에 반영했습니다.`);
    }

    reasons.push(`관련 후기 ${sources.length}개를 근거로 답변했습니다.`);

    return reasons;
  }

  private toVectorLiteral(embedding: number[]) {
    if (embedding.length !== 1536) {
      throw new InternalServerErrorException(
        `Embedding dimension must be 1536, received ${embedding.length}`,
      );
    }

    return `[${embedding.join(',')}]`;
  }
}
```

## 10. RAG 컨트롤러 추가

파일 생성:

```text
apps/nest-api/src/rag/rag.controller.ts
```

```ts
import { Body, Controller, Param, Post } from '@nestjs/common';
import { AskRagQuestionDto } from './dto/ask-rag-question.dto';
import { RagService } from './rag.service';

@Controller('rag')
export class RagController {
  constructor(private readonly ragService: RagService) {}

  @Post('questions')
  ask(@Body() dto: AskRagQuestionDto) {
    return this.ragService.ask(dto.question, dto.limit);
  }

  @Post('index/:reviewId')
  indexOne(@Param('reviewId') reviewId: string) {
    return this.ragService.upsertReviewEmbedding(BigInt(reviewId));
  }

  @Post('index')
  indexAll() {
    return this.ragService.reindexAll();
  }
}
```

`POST /rag/index`와 `POST /rag/index/:reviewId`는 MVP용 관리자 기능이다.
운영 단계에서는 관리자 인증 Guard를 붙이거나 CLI/worker 전용으로 빼야 한다.

## 11. RAG 모듈 추가

파일 생성:

```text
apps/nest-api/src/rag/rag.module.ts
```

```ts
import { Module } from '@nestjs/common';
import { DatabaseModule } from 'src/database/database.module';
import { OpenAiRagClient } from './openai-rag.client';
import { RagController } from './rag.controller';
import { RagQueryParser } from './rag-query-parser';
import { RagService } from './rag.service';

@Module({
  imports: [DatabaseModule],
  controllers: [RagController],
  providers: [OpenAiRagClient, RagQueryParser, RagService],
  exports: [RagService],
})
export class RagModule {}
```

## 12. AppModule에 RAG 모듈 연결

파일 수정:

```text
apps/nest-api/src/app.module.ts
```

```ts
import { Module } from '@nestjs/common';
import { ConfigModule } from '@nestjs/config';
import { AuthModule } from './auth/auth.module';
import { CommentsModule } from './comments/comments.module';
import { DatabaseModule } from './database/database.module';
import { HealthModule } from './health/health.module';
import { MetadataModule } from './metadata/metadata.module';
import { RagModule } from './rag/rag.module';
import { SeatReviewsModule } from './seat-reviews/seat-reviews.module';
import { TagsModule } from './tags/tags.module';

@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true }),
    DatabaseModule,
    HealthModule,
    AuthModule,
    MetadataModule,
    SeatReviewsModule,
    CommentsModule,
    TagsModule,
    RagModule,
  ],
})
export class AppModule {}
```

## 13. 후기 생성/수정/삭제와 인덱싱 연결

후기 작성 직후 임베딩을 만들되, 임베딩 실패 때문에 후기 작성까지 실패시키지는 않는다.
사용자는 후기를 저장했고, RAG 인덱싱은 나중에 재시도할 수 있기 때문이다.

파일 수정:

```text
apps/nest-api/src/seat-reviews/seat-reviews.module.ts
```

```ts
import { Module } from '@nestjs/common';
import { DatabaseModule } from 'src/database/database.module';
import { RagModule } from 'src/rag/rag.module';
import { SeatReviewsController } from './seat-reviews.controller';
import { SeatReviewsService } from './seat-reviews.service';

@Module({
  imports: [DatabaseModule, RagModule],
  controllers: [SeatReviewsController],
  providers: [SeatReviewsService],
})
export class SeatReviewsModule {}
```

파일 수정:

```text
apps/nest-api/src/seat-reviews/seat-reviews.service.ts
```

생성자에 `RagService`를 주입한다.

```ts
import { RagService } from 'src/rag/rag.service';

@Injectable()
export class SeatReviewsService {
  constructor(
    private readonly prisma: PrismaService,
    private readonly ragService: RagService,
  ) {}

  // ...
}
```

`create`에서 리뷰 생성 후 반환 전에 인덱싱을 호출한다.

```ts
const review = await this.prisma.seatReview.create({
  data: {
    // 기존 create data 그대로 둔다.
  },
  include: seatReviewInclude,
});

void this.ragService.upsertReviewEmbedding(review.id).catch((error) => {
  console.error('Failed to index seat review embedding', error);
});

return this.toPublicReview(review);
```

`update`에서도 수정된 리뷰 기준으로 다시 인덱싱한다.

```ts
const updateReview = await this.prisma.seatReview.update({
  where: { id: reviewId },
  data: {
    // 기존 update data 그대로 둔다.
  },
  include: seatReviewInclude,
});

void this.ragService.upsertReviewEmbedding(updateReview.id).catch((error) => {
  console.error('Failed to reindex seat review embedding', error);
});

return this.toPublicReview(updateReview);
```

`remove`에서는 삭제 전에 임베딩을 지워도 되고, FK `ON DELETE CASCADE`에 맡겨도 된다.
명시적으로 정리하려면 삭제 트랜잭션에 아래 호출을 추가한다.

```ts
await this.prisma.$transaction([
  this.prisma.seatReviewEmbedding.deleteMany({
    where: { seatReviewId: reviewId },
  }),
  this.prisma.seatReviewTag.deleteMany({
    where: { seatReviewId: reviewId },
  }),
  this.prisma.comment.deleteMany({ where: { seatReviewId: reviewId } }),
  this.prisma.seatReview.delete({ where: { id: reviewId } }),
]);
```

주의:

- 현재 Prisma 모델에 `SeatReviewEmbedding`을 추가해야 `this.prisma.seatReviewEmbedding`이 생긴다.
- Prisma에서 `Unsupported("vector(1536)")` 컬럼은 일반 create/update가 불편하므로 임베딩 저장 자체는 `RagService`의 raw SQL을 사용한다.

기존 `apps/nest-api/src/seat-reviews/seat-reviews.service.spec.ts`는 `SeatReviewsService` 생성자 인자가 늘어나므로 같이 수정한다.

```ts
const makeRagService = () => ({
  upsertReviewEmbedding: jest.fn().mockResolvedValue({ indexed: true }),
  deleteReviewEmbedding: jest.fn().mockResolvedValue(undefined),
});

// 기존
const service = new SeatReviewsService(prisma as never);

// 변경
const ragService = makeRagService();
const service = new SeatReviewsService(prisma as never, ragService as never);
```

후기 생성 테스트에는 인덱싱 호출 여부도 하나 확인해 둔다.

```ts
expect(ragService.upsertReviewEmbedding).toHaveBeenCalledWith(11n);
```

## 14. 기존 후기 임베딩 백필 스크립트 추가

이미 DB에 들어 있는 후기는 자동으로 임베딩이 생기지 않는다.
한 번 전체 백필을 실행해야 한다.

파일 생성:

```text
apps/nest-api/prisma/backfill-rag-embeddings.ts
```

```ts
import { NestFactory } from '@nestjs/core';
import { AppModule } from '../src/app.module';
import { RagService } from '../src/rag/rag.service';

async function main() {
  const app = await NestFactory.createApplicationContext(AppModule, {
    logger: ['error', 'warn', 'log'],
  });

  try {
    const ragService = app.get(RagService);
    const result = await ragService.reindexAll();
    console.log(`Indexed ${result.indexedCount} seat reviews.`);
  } finally {
    await app.close();
  }
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
```

`apps/nest-api/package.json` scripts에 추가한다.

```json
{
  "scripts": {
    "rag:backfill": "ts-node prisma/backfill-rag-embeddings.ts"
  }
}
```

실행:

```bash
cd apps/nest-api
npm run rag:backfill
```

## 15. API 테스트

서버 실행:

```bash
cd apps/nest-api
npm run start:dev
```

전체 인덱싱:

```bash
curl -X POST http://localhost:3000/rag/index
```

질문:

```bash
curl -X POST http://localhost:3000/rag/questions \
  -H "Content-Type: application/json" \
  -d "{\"question\":\"블루스퀘어 2층 3열은 시야 괜찮아?\",\"limit\":5}"
```

예상 응답 모양:

```json
{
  "answer": "블루스퀘어 2층 3열은 전체 무대를 보기에는 무난하지만...",
  "reasons": [
    "블루스퀘어 극장 후기를 우선 검색했습니다.",
    "view 의도에 맞는 평점 기준을 검색 정렬에 반영했습니다.",
    "관련 후기 5개를 근거로 답변했습니다."
  ],
  "filters": {
    "theaterId": "1",
    "theaterName": "블루스퀘어",
    "seatFloor": "2층",
    "seatRow": "3",
    "intent": "view"
  },
  "sources": [
    {
      "id": "12",
      "score": 0.8123,
      "theater": {
        "id": "1",
        "name": "블루스퀘어"
      },
      "musical": {
        "id": "4",
        "title": "어쩌면 해피엔딩"
      },
      "performance": {
        "id": "7",
        "seasonLabel": "2026"
      },
      "seat": {
        "floor": "2층",
        "section": "중앙",
        "row": "3",
        "number": "12"
      },
      "ratings": {
        "view": 4,
        "sound": 4,
        "comfort": 3,
        "expression": 2,
        "stageVisibility": 5
      },
      "tags": ["전체무대잘보임"],
      "content": "2층 앞열이라 전체 무대는 잘 보였지만 표정은 조금 멀었습니다."
    }
  ]
}
```

## 16. React API 함수 추가

백엔드가 먼저 동작하면 React에서는 API 함수와 타입부터 추가한다.

파일 생성:

```text
apps/web-react/src/features/rag/types.ts
```

```ts
export type RagIntent =
  | "general"
  | "view"
  | "sound"
  | "comfort"
  | "expression"
  | "stageVisibility"

export type RagQuestionFilters = {
  theaterId?: string
  theaterName?: string
  musicalId?: string
  musicalTitle?: string
  seatFloor?: string
  seatSection?: string
  seatRow?: string
  seatNumber?: string
  side?: "left" | "center" | "right"
  intent: RagIntent
}

export type RagSource = {
  id: string
  score: number
  theater: {
    id: string
    name: string
  }
  musical: {
    id: string
    title: string
  }
  performance: {
    id: string
    seasonLabel: string | null
  } | null
  seat: {
    floor: string
    section: string | null
    row: string
    number: string
  }
  ratings: {
    view: number
    sound: number
    comfort: number
    expression: number
    stageVisibility: number
  }
  tags: string[]
  content: string
}

export type RagAnswer = {
  answer: string
  reasons: string[]
  filters: RagQuestionFilters
  sources: RagSource[]
}
```

파일 생성:

```text
apps/web-react/src/features/rag/api.ts
```

```ts
import { apiRequest } from "../../shared/api"
import type { RagAnswer } from "./types"

export function askRagQuestion(input: { question: string; limit?: number }) {
  return apiRequest<RagAnswer>("/rag/questions", {
    method: "POST",
    body: JSON.stringify(input),
  })
}
```

화면은 나중에 붙여도 된다.
이 단계에서는 브라우저 UI보다 API가 먼저 정확해야 한다.

## 17. 최소 단위 테스트 추가

RAG 전체는 외부 API와 DB 벡터 검색이 섞이므로 처음부터 완벽한 단위 테스트를 만들기 어렵다.
대신 비용이 들지 않는 조건 추출기부터 테스트한다.

파일 생성:

```text
apps/nest-api/src/rag/rag-query-parser.spec.ts
```

```ts
import { RagQueryParser } from './rag-query-parser';

describe('RagQueryParser', () => {
  const makePrisma = () => ({
    theater: {
      findMany: jest.fn().mockResolvedValue([
        { id: 1n, name: '블루스퀘어' },
        { id: 2n, name: '샤롯데씨어터' },
      ]),
    },
    musical: {
      findMany: jest.fn().mockResolvedValue([
        { id: 10n, title: '어쩌면 해피엔딩' },
        { id: 11n, title: '지킬앤하이드' },
      ]),
    },
  });

  it('extracts theater, musical, seat, side, and intent', async () => {
    const parser = new RagQueryParser(makePrisma() as never);

    await expect(
      parser.parse('블루스퀘어 어쩌면 해피엔딩 2층 중앙블록 3열은 표정 잘 보여?'),
    ).resolves.toMatchObject({
      theaterId: '1',
      theaterName: '블루스퀘어',
      musicalId: '10',
      musicalTitle: '어쩌면 해피엔딩',
      seatFloor: '2층',
      seatSection: '중앙',
      seatRow: '3',
      side: 'center',
      intent: 'expression',
    });
  });

  it('classifies comfort questions', async () => {
    const parser = new RagQueryParser(makePrisma() as never);

    await expect(parser.parse('샤롯데씨어터 1층은 오래 앉아 있기 편해?')).resolves.toMatchObject({
      theaterId: '2',
      intent: 'comfort',
    });
  });
});
```

테스트 실행:

```bash
cd apps/nest-api
npm test -- rag-query-parser
```

## 18. 구현 중 자주 나는 오류

### `type "vector" does not exist`

DB에 `pgvector` 확장이 없다.

확인:

```sql
SELECT * FROM pg_available_extensions WHERE name = 'vector';
```

해결:

- Postgres 이미지에 pgvector를 설치한다.
- 또는 Docker 이미지를 `pgvector/pgvector` 계열로 바꾼다.

### `Embedding dimension must be 1536`

DB 컬럼은 `vector(1536)`인데 다른 차원의 임베딩을 받은 것이다.

해결:

- `OPENAI_EMBEDDING_MODEL=text-embedding-3-small`을 사용한다.
- 다른 모델 또는 `dimensions` 값을 쓰려면 DB 컬럼 차원도 같이 바꾼다.

### `OPENAI_API_KEY is not set`

Nest API 실행 환경에 키가 없다.

해결:

- `apps/nest-api/.env`에 `OPENAI_API_KEY`를 추가한다.
- `ConfigModule.forRoot({ isGlobal: true })`가 이미 `AppModule`에 있으므로 서버 재시작 후 적용된다.

### 검색 결과가 0개다

가능한 원인:

- `npm run rag:backfill`을 실행하지 않았다.
- 후기 생성은 됐지만 임베딩 생성이 실패했다.
- 질문에서 추출한 `seatFloor` 값과 DB 저장값이 다르다. 예: 질문은 `2층`, DB는 `2F`.
- 작품명/극장명이 DB 이름과 정확히 다르다.

먼저 아래 SQL로 임베딩 개수를 확인한다.

```sql
SELECT COUNT(*) FROM seat_review_embeddings;
```

## 19. 품질 개선은 다음 단계에서 한다

이번 단계는 “작동하는 RAG”를 만드는 것이 목표다.
아래 개선은 step 2의 `008_rag_quality_and_evaluation.md`에서 다룬다.

- 질문 조건 추출을 LLM structured output으로 바꾸기
- 답변 품질 평가셋 만들기
- hallucination 방지 테스트 추가
- 인덱싱을 큐/worker로 분리하기
- 답변 캐시 추가
- 추천 피드백 저장하기
- `FastAPI` 모델 서버로 분리하기

## 최종 체크리스트

- [ ] `pgvector` 확장이 설치되어 있다.
- [ ] `seat_review_embeddings` 테이블이 생성되었다.
- [ ] `schema.prisma`에 `SeatReviewEmbedding` 모델이 추가되었다.
- [ ] `RagModule`이 `AppModule`에 연결되었다.
- [ ] `POST /rag/index`로 기존 후기를 인덱싱할 수 있다.
- [ ] 후기 생성 후 임베딩이 자동으로 저장된다.
- [ ] 후기 수정 후 임베딩이 다시 저장된다.
- [ ] 후기 삭제 후 임베딩이 같이 삭제된다.
- [ ] `POST /rag/questions`가 답변과 근거 후기를 반환한다.
- [ ] 극장/작품/좌석/의도 조건이 `filters`에 보인다.
- [ ] RAG 결과가 없을 때도 사용자에게 안전한 답변을 반환한다.
- [ ] `npm run build`가 통과한다.
- [ ] `npm test -- rag-query-parser`가 통과한다.
