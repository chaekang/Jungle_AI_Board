# Nest API

This folder contains the existing NestJS backend for the `agentic-board` workspace.

## Run

```powershell
cd apps/nest-api
npm install
npm run start:dev
```

## Test

```powershell
cd apps/nest-api
npm test
npm run test:e2e
```
